import 'package:flutter/material.dart';

class HomeAssistantCamera extends StatelessWidget {
  const HomeAssistantCamera({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
