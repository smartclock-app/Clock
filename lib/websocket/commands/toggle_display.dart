// This file was moved to `lib/remote/commands/toggle_display.dart` as part of
// the remoteConfig transport migration. The original file is kept as a stub
// to avoid breaking imports that still reference the old location.

// Use `package:smartclock/remote/commands/toggle_display.dart` instead.
